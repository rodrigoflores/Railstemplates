#create-template.rounded-all
  %h2 Step 2
  = semantic_form_for @template, :url => gists_path do |form|
    = form.inputs do
      = form.input :gist_file, :as => :select, :collection => @github.gist_files, :prompt => false
      = form.input :title, :input_html => { :placeholder => "Title" }
      = form.input :description, :as => :text, :input_html => { :placeholder => "Description" }
      = form.input :tag_list, :input_html => { :placeholder => "Tag List" }
    = form.buttons do
      = form.submit 'Create!', :class => "rounded-all"