#current_githubber
  - if githubber.email
    = gravatar_for githubber
  %p
    Hello,
    %em= githubber.name
  = link_to "Sign out", destroy_githubber_session_path, :class => "rounded-all"

%ul#menu
  %li= link_to "Dashboard", dashboard_path, :class => "rounded-all"
  %li= link_to "Create a new template", new_template_path, :class => "rounded-all"
  %li= link_to "All templates", templates_path, :class => "rounded-all"
  %li= link_to "Learn more", learn_path, :class => "rounded-all"
  