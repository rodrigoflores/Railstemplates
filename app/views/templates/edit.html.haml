= semantic_form_for @template, :url => template_path do |form|
  %h4
    Your template file:
    %strong= @template.gist_file
  = form.inputs do
    = form.input :title
    = form.input :description
  = form.buttons do
    = form.commit_button