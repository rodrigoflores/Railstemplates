- if current_githubber
  %p 
    Logged in as 
    = "#{current_githubber.name} <#{current_githubber.email}>"
    = link_to "Log out", destroy_githubber_session_path
- else
  Not logged in 
  \(
  = link_to "Login", new_githubber_session_path
  \)

    