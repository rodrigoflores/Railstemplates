%table
  - @templates.each do |template|
    %tr
      %td= link_to template.title, template
      %td= link_to 'Edit', edit_template_path(template), :class => "edit-template"
      %td= link_to 'Delete', template_path(template), :method => :delete, :class => "delete-template"

= will_paginate @templates