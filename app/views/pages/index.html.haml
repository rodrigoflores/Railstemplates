#holder-home
  #home-message
    %h1.replacement Take a shortcut to your templates
    %h2.replacement Find the best to kick off your project
#explanation
  %p Share, find and rate. participate with other developers to build and customize the best templates for your Rails apps
#latest
  %h2 # Most Downloaded
  %ul
    - @latest.each do |template|
      %li{ :class => cycle( "one", "two")}
        = link_to template.title, template
        %span
          %em{:title => l(template.created_at, :format => :long)} -
          = time_ago_in_words(template.created_at) + " ago"

#ranked
  %h2 # Most Liked
  %ul
    - @ranked.each do |ranked|
      %li{ :class => cycle( "one", "two")}
        = link_to ranked.title, ranked
        %span= pluralize(ranked.likes.count, "Like")